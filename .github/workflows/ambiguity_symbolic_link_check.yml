name: 🔗 Symbolic Link Simulation Check

on:
  push:
    paths:
      - 'microservices/ambiguity/ambiguity_resolver.py'
  workflow_dispatch:

jobs:
  run-symbolic-link-check:
    runs-on: ubuntu-latest
    steps:
      - name: Create a symbolic alias for ambiguity_resolver
        run: |
          ln -sf ambiguity_resolver.py microservices/ambiguity/resolver_alias.py
          echo "✅ Symbolic link resolver_alias.py created"
